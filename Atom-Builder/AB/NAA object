NAA objectparameters rect of boxproperties:	countUpQuarks			countDownQuarks			boxRect			methods:	dropQuark			reset			kill (?)			on new me	set up the rect	set countUpQuarks = 0	set countDownQuarks = 0end newon dropQuark me, type   if it's in the box, do your stuff		sfx("particle drop")		if type = "up" then set countUpQuarks = countUpQuarks + 1         if it's not, return falseend dropQuark