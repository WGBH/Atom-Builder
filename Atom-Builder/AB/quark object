upQuark objectmethods: grab		 drop		 kill		 public properties: placed (boolean)on new me	ancestor(upQuarkWell)		check to see if there are enough up quarks remaining	if yes		set countUpQuarksInWell to countUpQuarksInWell - 1		find an available sprite		set placed = false		set the member of the sprite		set the visible of the sprite		set the mouse!        return me	if not		exit	end if	end newon grab	if not placed,	move the quark to the mouseend grab on drop    drop the quark    send event to NAAobjectend dropon kill    return sprite to original stateend kill[pretty much the same for all particles, except some send events to atom instead of NAA]